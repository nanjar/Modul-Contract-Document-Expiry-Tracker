'use client';
import { useState } from 'react';
export default function SettingsPage(){const [name,setName]=useState('Expiry Tracker'),[days,setDays]=useState('30'),[saved,setSaved]=useState(false);return <main className="app-page"><div className="page-head"><div><div className="eyebrow">ADMINISTRATION</div><h1>Settings</h1><p>Keep default expiry and reminder behavior understandable.</p></div></div><section className="panel settings-form"><label>Business / application name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Default expiry warning threshold (days)<input type="number" min="1" max="365" value={days} onChange={e=>setDays(e.target.value)}/></label><div className="settings-note"><strong>Notification provider</strong><span>Email provider configuration is environment-managed. Secrets are never stored in the UI.</span></div><button className="primary" onClick={()=>setSaved(true)}>Save settings</button>{saved&&<div className="success">Settings saved for this session. Persisted system settings are the next backend hardening step.</div>}</section></main>}
