'use client';
import { FormEvent, useEffect, useState } from 'react'; import { api } from '../../lib/api';
type User={id:string;email:string;name:string;role:string;isActive:boolean};
export default function UsersPage(){const [users,setUsers]=useState<User[]>([]),[open,setOpen]=useState(false),[error,setError]=useState('');async function load(){try{setUsers(await api('/users'))}catch(e){setError(e instanceof Error?e.message:'Unable to load users')}}useEffect(()=>{load()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/users',{method:'POST',body:JSON.stringify({email:f.get('email'),name:f.get('name'),password:f.get('password'),role:f.get('role')})});setOpen(false);load()}catch(e){setError(e instanceof Error?e.message:'Unable to create user')}}return <main className="app-page"><div className="page-head"><div><div className="eyebrow">ADMINISTRATION</div><h1>Users</h1><p>Control access with clear, server-enforced roles.</p></div><button className="primary" onClick={()=>setOpen(true)}>+ Add user</button></div>{error&&<div className="error">{error}</div>}<section className="panel"><div className="table user-table">{users.map(u=><div className="row" key={u.id}><div><strong>{u.name}</strong><span>{u.email}</span></div><strong>{u.role}</strong><span className={u.isActive?'status active':'status expired'}>{u.isActive?'Active':'Inactive'}</span></div>)}</div></section>{open&&<div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><h2>Add user</h2><button type="button" className="icon-button" onClick={()=>setOpen(false)}>×</button></div><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Temporary password<input name="password" type="password" minLength={8} required/></label><label>Role<select name="role" defaultValue="VIEWER"><option>VIEWER</option><option>EDITOR</option><option>SUPERUSER</option></select></label><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="primary">Create user</button></div></form></div>}</main>}
