'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';
import Link from 'next/link';

type Lang = 'en' | 'id';
type Theme = 'dark' | 'light';
type Role = 'SUPERUSER' | 'EDITOR' | 'VIEWER';
type Status = 'ACTIVE' | 'EXPIRING_SOON' | 'EXPIRED' | 'NO_EXPIRY' | 'ARCHIVED';
type Summary = { total: number; active: number; expiringSoon: number; expired: number; noExpiry: number };
type DocumentItem = { id: string; title: string; documentType: string; counterparty?: string | null; expiryDate?: string | null; status: Status; updatedAt?: string; createdAt?: string };

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001/api/v1';
const emptySummary: Summary = { total: 0, active: 0, expiringSoon: 0, expired: 0, noExpiry: 0 };

const copy = {
  en: {
    product: 'Contract & Document Expiry Tracker', secure: 'SECURE WORKSPACE', welcome: 'Welcome back', signInAccount: 'Sign in to continue to your workspace.', email: 'Email', password: 'Password', signIn: 'Sign in', signingIn: 'Signing in…', loginFailed: 'Login failed', protected: 'Protected by JWT authentication and server-side role enforcement.',
    overview: 'Overview', dashboardTitle: 'Overview', dashboardCopy: 'Monitor important documents and expirations at a glance.', add: 'Add document', total: 'Total documents', active: 'Active', expiringSoon: 'Expiring soon', expired: 'Expired', noExpiry: 'No expiry', attention: 'Expiring soon', attentionSub: 'Documents requiring your attention', recent: 'Recent documents', status: 'Documents by status', timeline: 'Expiry timeline', categories: 'Document categories', quick: 'Quick actions', viewAll: 'View all', allClear: 'All clear', noAttention: 'No expired or soon-to-expire documents found.', noDocs: 'No documents yet.', addFirst: 'Add your first document to start tracking expiry dates.', days: 'days', day: 'day', expiredLabel: 'expired', noExpiryLabel: 'No expiry', search: 'Search documents…', logout: 'Sign out', light: 'Light', dark: 'Dark', activeDocuments: 'active documents', warningWindow: 'outside the 30-day warning window', needsAttention: 'need attention', basedOnStatus: 'based on current expiry status', upload: 'Upload document', searchAction: 'Search documents', roleAdmin: 'Administration', users: 'Users', audit: 'Audit log', settings: 'Settings', documents: 'Documents', reminders: 'Reminders', archived: 'Archived', command: 'Command center', language: 'Language', expiredNow: 'Require immediate action', within30: 'Within 30 days', allActive: 'All active documents', updated: 'Updated', owner: 'Owner', category: 'Category', expiry: 'Expiry date', actions: 'Actions', statusActive: 'Active', statusExpiring: 'Expiring soon', statusExpired: 'Expired', statusArchived: 'Archived', noExpiryStatus: 'No expiry'
  },
  id: {
    product: 'Contract & Document Expiry Tracker', secure: 'RUANG KERJA AMAN', welcome: 'Selamat datang kembali', signInAccount: 'Masuk untuk melanjutkan ke workspace Anda.', email: 'Email', password: 'Kata sandi', signIn: 'Masuk', signingIn: 'Sedang masuk…', loginFailed: 'Login gagal', protected: 'Dilindungi autentikasi JWT dan penegakan role di sisi server.',
    overview: 'Ringkasan', dashboardTitle: 'Ringkasan', dashboardCopy: 'Pantau dokumen penting dan masa berlaku dalam satu tampilan.', add: 'Tambah dokumen', total: 'Total dokumen', active: 'Aktif', expiringSoon: 'Segera berakhir', expired: 'Sudah berakhir', noExpiry: 'Tanpa masa berlaku', attention: 'Segera berakhir', attentionSub: 'Dokumen yang membutuhkan perhatian', recent: 'Dokumen terbaru', status: 'Dokumen berdasarkan status', timeline: 'Timeline masa berlaku', categories: 'Kategori dokumen', quick: 'Aksi cepat', viewAll: 'Lihat semua', allClear: 'Semua aman', noAttention: 'Tidak ada dokumen yang sudah atau segera berakhir.', noDocs: 'Belum ada dokumen.', addFirst: 'Tambahkan dokumen pertama untuk mulai memantau masa berlaku.', days: 'hari', day: 'hari', expiredLabel: 'terlambat', noExpiryLabel: 'Tanpa masa berlaku', search: 'Cari dokumen…', logout: 'Keluar', light: 'Terang', dark: 'Gelap', activeDocuments: 'dokumen aktif', warningWindow: 'di luar jendela peringatan 30 hari', needsAttention: 'perlu perhatian', basedOnStatus: 'berdasarkan status masa berlaku', upload: 'Upload dokumen', searchAction: 'Cari dokumen', roleAdmin: 'Administrasi', users: 'Pengguna', audit: 'Log audit', settings: 'Pengaturan', documents: 'Dokumen', reminders: 'Pengingat', archived: 'Diarsipkan', command: 'Command center', language: 'Bahasa', expiredNow: 'Perlu tindakan segera', within30: 'Dalam 30 hari', allActive: 'Semua dokumen aktif', updated: 'Diperbarui', owner: 'Pemilik', category: 'Kategori', expiry: 'Tanggal berakhir', actions: 'Aksi', statusActive: 'Aktif', statusExpiring: 'Segera berakhir', statusExpired: 'Sudah berakhir', statusArchived: 'Diarsipkan', noExpiryStatus: 'Tanpa masa berlaku'
  }
} as const;
type Key = keyof typeof copy.en;

function Icon({ name, size = 18 }: { name: string; size?: number }) {
  const p = { width: size, height: size, viewBox: '0 0 24 24', fill: 'none', stroke: 'currentColor', strokeWidth: 1.8, strokeLinecap: 'round' as const, strokeLinejoin: 'round' as const };
  if (name === 'home') return <svg {...p}><path d="m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1V10Z" /></svg>;
  if (name === 'doc') return <svg {...p}><path d="M6 3h8l4 4v14H6z" /><path d="M14 3v5h5M9 12h6M9 16h5" /></svg>;
  if (name === 'bell') return <svg {...p}><path d="M18 9a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4" /></svg>;
  if (name === 'archive') return <svg {...p}><path d="M4 7h16v13H4zM3 4h18v3H3zM9 11h6" /></svg>;
  if (name === 'audit') return <svg {...p}><path d="M7 3h10v18H7zM10 7h4M10 11h4M10 15h3" /></svg>;
  if (name === 'users') return <svg {...p}><circle cx="9" cy="8" r="3" /><path d="M3.5 20a5.5 5.5 0 0 1 11 0M16 5a3 3 0 0 1 0 6M17 14a4.5 4.5 0 0 1 4 6" /></svg>;
  if (name === 'settings') return <svg {...p}><circle cx="12" cy="12" r="3" /><path d="M19.4 15a1.8 1.8 0 0 0 .4 2l.1.1-1.8 1.8-.1-.1a1.8 1.8 0 0 0-2-.4 1.8 1.8 0 0 0-1.1 1.7v.2h-2.6v-.2a1.8 1.8 0 0 0-1.1-1.7 1.8 1.8 0 0 0-2 .4l-.1.1-1.8-1.8.1-.1a1.8 1.8 0 0 0 .4-2A1.8 1.8 0 0 0 6 13.9h-.2v-2.6H6a1.8 1.8 0 0 0 1.7-1.1 1.8 1.8 0 0 0-.4-2l-.1-.1L9 6.3l.1.1a1.8 1.8 0 0 0 2 .4A1.8 1.8 0 0 0 12.2 5v-.2h2.6V5a1.8 1.8 0 0 0 1.1 1.7 1.8 1.8 0 0 0 2-.4l.1-.1 1.8 1.8-.1.1a1.8 1.8 0 0 0-.4 2 1.8 1.8 0 0 0 1.7 1.1h.2v2.6H21a1.8 1.8 0 0 0-1.6 1.2Z" /></svg>;
  if (name === 'search') return <svg {...p}><circle cx="11" cy="11" r="6.5" /><path d="m16 16 4.5 4.5" /></svg>;
  if (name === 'clock') return <svg {...p}><circle cx="12" cy="12" r="9" /><path d="M12 7v5l3 2" /></svg>;
  if (name === 'alert') return <svg {...p}><path d="m12 3 9 17H3L12 3Z" /><path d="M12 9v5M12 17h.01" /></svg>;
  if (name === 'plus') return <svg {...p}><path d="M12 5v14M5 12h14" /></svg>;
  if (name === 'upload') return <svg {...p}><path d="M12 16V4M8 8l4-4 4 4M5 20h14" /></svg>;
  return <svg {...p}><circle cx="12" cy="12" r="9" /></svg>;
}

function Brand() { return <span className="dash-brand-mark"><svg viewBox="0 0 48 48" fill="none"><rect x="2" y="2" width="44" height="44" rx="13" fill="#4C6FFF"/><path d="M16 11h11l7 7v19H16V11Z" fill="white"/><path d="M27 11v8h8" fill="white" fillOpacity=".7"/><path d="m24 24 2.8 2.8L33 20.6" stroke="#4E6BFF" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"/></svg></span>; }

function decodeToken(token: string | null): { role: Role; name: string; email?: string } {
  try {
    if (!token) return { role: 'VIEWER', name: 'User' };
    const payload = JSON.parse(atob(token.split('.')[1].replace(/-/g, '+').replace(/_/g, '/')));
    return { role: payload.role ?? 'VIEWER', name: payload.name ?? payload.email ?? 'User', email: payload.email };
  } catch { return { role: 'VIEWER', name: 'User' }; }
}

function dateValue(v?: string | null) { return v ? new Date(v).getTime() : Number.POSITIVE_INFINITY; }
function formatDate(v: string | null | undefined, lang: Lang) { if (!v) return lang === 'id' ? 'Tanpa masa berlaku' : 'No expiry'; return new Intl.DateTimeFormat(lang === 'id' ? 'id-ID' : 'en-US', { day: '2-digit', month: 'short', year: 'numeric' }).format(new Date(v)); }
function daysUntil(v?: string | null) { if (!v) return null; return Math.ceil((new Date(v).getTime() - new Date().setHours(0, 0, 0, 0)) / 86400000); }
function statusLabel(status: Status, t: (k: Key) => string) { if (status === 'ACTIVE') return t('statusActive'); if (status === 'EXPIRING_SOON') return t('statusExpiring'); if (status === 'EXPIRED') return t('statusExpired'); if (status === 'ARCHIVED') return t('statusArchived'); return t('noExpiryStatus'); }
function initials(name: string) { return name.split(/\s+/).filter(Boolean).slice(0, 2).map(v => v[0]).join('').toUpperCase() || 'US'; }

function Feature({ icon, title, text }: { icon: string; title: string; text: string }) { return <div className="login-feature"><span><Icon name={icon} size={18}/></span><div><strong>{title}</strong><small>{text}</small></div></div>; }

export default function Home() {
  const [lang, setLang] = useState<Lang>('en');
  const [theme, setTheme] = useState<Theme>('light');
  const [token, setToken] = useState<string | null>(null);
  const [role, setRole] = useState<Role>('VIEWER');
  const [userName, setUserName] = useState('User');
  const [email, setEmail] = useState('admin@example.com');
  const [password, setPassword] = useState('Admin123!');
  const [showPassword, setShowPassword] = useState(false);
  const [summary, setSummary] = useState(emptySummary);
  const [expiring, setExpiring] = useState<DocumentItem[]>([]);
  const [recent, setRecent] = useState<DocumentItem[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');
  const t = (k: Key) => copy[lang][k];

  useEffect(() => {
    const storedToken = sessionStorage.getItem('expiry-tracker-token');
    const storedLang = localStorage.getItem('expiry-tracker-language') as Lang | null;
    const storedTheme = localStorage.getItem('expiry-tracker-theme') as Theme | null;
    if (storedToken) { setToken(storedToken); const u = decodeToken(storedToken); setRole(u.role); setUserName(u.name); }
    if (storedLang === 'en' || storedLang === 'id') setLang(storedLang);
    if (storedTheme === 'dark' || storedTheme === 'light') setTheme(storedTheme);
  }, []);

  function changeLang(value: Lang) { setLang(value); localStorage.setItem('expiry-tracker-language', value); }
  function changeTheme(value: Theme) { setTheme(value); localStorage.setItem('expiry-tracker-theme', value); }

  useEffect(() => {
    if (!token) return;
    const load = async () => {
      setLoading(true); setError('');
      try {
        const headers = { Authorization: `Bearer ${token}` };
        const [a, b, c] = await Promise.all([
          fetch(`${API_URL}/dashboard/summary`, { headers }),
          fetch(`${API_URL}/dashboard/expiring?limit=8`, { headers }),
          fetch(`${API_URL}/dashboard/recent?limit=8`, { headers })
        ]);
        if ([a, b, c].some(r => r.status === 401)) { sessionStorage.removeItem('expiry-tracker-token'); setToken(null); return; }
        if (!a.ok || !b.ok || !c.ok) throw new Error('Unable to load dashboard data');
        setSummary(await a.json()); setExpiring(await b.json()); setRecent(await c.json());
      } catch (e) { setError(e instanceof Error ? e.message : 'Unable to load dashboard data'); }
      finally { setLoading(false); }
    };
    load();
  }, [token]);

  async function login(e: FormEvent) {
    e.preventDefault(); setLoading(true); setError('');
    try {
      const response = await fetch(`${API_URL}/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) });
      const payload = await response.json();
      if (!response.ok) throw new Error(payload.message ?? t('loginFailed'));
      sessionStorage.setItem('expiry-tracker-token', payload.accessToken);
      const u = decodeToken(payload.accessToken); setToken(payload.accessToken); setRole(payload.user?.role ?? u.role); setUserName(payload.user?.name ?? u.name);
      window.dispatchEvent(new Event('expiry-tracker-auth-change'));
    } catch (e) { setError(e instanceof Error ? e.message : t('loginFailed')); }
    finally { setLoading(false); }
  }

  function logout() { sessionStorage.removeItem('expiry-tracker-token'); setToken(null); setRole('VIEWER'); setUserName('User'); setSummary(emptySummary); setExpiring([]); setRecent([]); window.dispatchEvent(new Event('expiry-tracker-auth-change')); }

  const timeline = useMemo(() => {
    const buckets = [0, 0, 0, 0];
    [...expiring, ...recent].forEach(doc => { const d = daysUntil(doc.expiryDate); if (d === null || d < 0) return; if (d <= 7) buckets[0]++; else if (d <= 30) buckets[1]++; else if (d <= 60) buckets[2]++; else buckets[3]++; });
    const source = summary.expiringSoon > 0 && buckets.reduce((a, b) => a + b, 0) === 0 ? [Math.min(summary.expiringSoon, 4), Math.max(summary.expiringSoon - 4, 0), 0, 0] : buckets;
    const max = Math.max(...source, 1); return source.map((value, i) => ({ value, height: Math.max(12, Math.round((value / max) * 100)), label: i === 0 ? '0–7' : i === 1 ? '8–30' : i === 2 ? '31–60' : '> 60' }));
  }, [expiring, recent, summary.expiringSoon]);

  const statusTotal = Math.max(summary.total, 1);
  const statusGradient = `conic-gradient(#27b77a 0 ${(summary.active / statusTotal) * 100}%, #f59f20 ${(summary.active / statusTotal) * 100}% ${((summary.active + summary.expiringSoon) / statusTotal) * 100}%, #ef4444 ${((summary.active + summary.expiringSoon) / statusTotal) * 100}% ${((summary.active + summary.expiringSoon + summary.expired) / statusTotal) * 100}%, #aab4c3 ${((summary.active + summary.expiringSoon + summary.expired) / statusTotal) * 100}% 100%)`;
  const orderedRecent = useMemo(() => [...recent].sort((a, b) => dateValue(b.updatedAt ?? b.createdAt) - dateValue(a.updatedAt ?? a.createdAt)), [recent]);
  const attention = useMemo(() => [...expiring].sort((a, b) => dateValue(a.expiryDate) - dateValue(b.expiryDate)).slice(0, 5), [expiring]);
  const greeting = new Date().getHours() < 12 ? (lang === 'id' ? 'Selamat pagi' : 'Good morning') : new Date().getHours() < 18 ? (lang === 'id' ? 'Selamat siang' : 'Good afternoon') : (lang === 'id' ? 'Selamat sore' : 'Good evening');

  if (!token) return <main className="premium-login">
    <section className="login-showcase"><div className="login-grid"/><div className="login-orb orb-one"/><div className="login-orb orb-two"/><div className="login-showcase-inner"><div className="login-brand"><Brand/><span>{t('product')}</span></div><p className="login-eyebrow">{t('secure')}</p><h1>Never let an important document<br/><em>expire unnoticed.</em></h1><p className="login-lead">A premium command center for contracts, licenses, certificates, permits, insurance and every business document that matters.</p><div className="login-features"><Feature icon="clock" title="Expiry intelligence" text="Know what needs attention next."/><Feature icon="bell" title="Smart reminders" text="Get notified before deadlines."/><Feature icon="archive" title="One source of truth" text="Keep every document organized."/></div></div><div className="login-footer">© 2026 Contract & Document Expiry Tracker</div></section>
    <section className="login-panel"><div className="login-panel-top"><button onClick={() => changeLang(lang === 'en' ? 'id' : 'en')}>{lang.toUpperCase()}</button></div><div className="login-card"><p className="login-eyebrow">{t('secure')}</p><h2>{t('welcome')}</h2><p>{t('signInAccount')}</p><form onSubmit={login}><label>{t('email')}<div className="login-input"><Icon name="users" size={18}/><input type="email" value={email} onChange={e => setEmail(e.target.value)} required autoComplete="email"/></div></label><label>{t('password')}<div className="login-input"><Icon name="settings" size={18}/><input type={showPassword ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} required minLength={8} autoComplete="current-password"/><button type="button" onClick={() => setShowPassword(v => !v)}>{showPassword ? 'Hide' : 'Show'}</button></div></label>{error && <div className="login-error"><Icon name="alert" size={16}/>{error}</div>}<button className="login-submit" disabled={loading}>{loading ? t('signingIn') : t('signIn')} <span>→</span></button></form><div className="login-security"><span>✓</span>{t('protected')}</div></div></section>
  </main>;

  const bg = theme === 'dark' ? '#090e16' : '#f7f9fc';
  const surface = theme === 'dark' ? '#111822' : '#ffffff';
  const text = theme === 'dark' ? '#f5f7fb' : '#142038';
  const muted = theme === 'dark' ? '#8e9aac' : '#6b778c';
  const border = theme === 'dark' ? '#26313f' : '#e4e9f0';
  const sidebar = theme === 'dark' ? '#0b1119' : '#071a34';
  const initialsText = initials(userName);

  const nav = [
    ['overview', '/', 'home'], ['documents', '/documents', 'doc'], ['reminders', '/reminders', 'bell'], ['archived', '/documents?status=ARCHIVED', 'archive']
  ] as const;

  return <main className="premium-app" style={{ background: bg, color: text, ['--surface' as string]: surface, ['--border' as string]: border, ['--muted' as string]: muted }}>
    <aside className="premium-sidebar" style={{ background: sidebar }}><div className="premium-brand"><Brand/><div><strong>Contract &amp; Document</strong><span>Expiry Tracker</span></div></div><div className="sidebar-label">{lang === 'id' ? 'UMUM' : 'GENERAL'}</div><nav>{nav.map(([key, href, icon]) => <Link href={href} key={href} className={`premium-nav ${href === '/' ? 'active' : ''}`}><Icon name={icon}/><span>{t(key)}</span></Link>)}</nav>{role === 'SUPERUSER' && <><div className="sidebar-label admin-label">{t('roleAdmin')}</div><nav><Link href="/users" className="premium-nav"><Icon name="users"/><span>{t('users')}</span></Link><Link href="/audit" className="premium-nav"><Icon name="audit"/><span>{t('audit')}</span></Link><Link href="/settings" className="premium-nav"><Icon name="settings"/><span>{t('settings')}</span></Link></nav></>}<div className="sidebar-account"><div className="avatar">{initialsText}</div><div><strong>{userName}</strong><span>{role}</span></div><button onClick={logout} title={t('logout')}>↗</button></div></aside>
    <section className="premium-main"><header className="premium-header"><div className="header-search"><Icon name="search" size={17}/><input placeholder={t('search')} onKeyDown={e => { if (e.key === 'Enter') window.location.href = `/documents?search=${encodeURIComponent((e.currentTarget as HTMLInputElement).value)}`; }}/><kbd>/</kbd></div><div className="header-actions"><button onClick={() => changeTheme(theme === 'dark' ? 'light' : 'dark')}>{theme === 'dark' ? '☼' : '◐'}</button><button onClick={() => changeLang(lang === 'en' ? 'id' : 'en')}>{lang.toUpperCase()}</button><button className="notification"><Icon name="bell" size={18}/><b>{summary.expiringSoon}</b></button><div className="header-user"><div className="avatar">{initialsText}</div><div><strong>{userName}</strong><span>{role}</span></div><button onClick={logout}>⌄</button></div></div></header>
      <div className="premium-content"><div className="page-heading"><div><p>{t('overview')}</p><h1>{greeting}, {userName.split(' ')[0]}.</h1><span>{t('dashboardCopy')}</span></div>{(role === 'SUPERUSER' || role === 'EDITOR') && <Link href="/documents/new" className="add-button"><Icon name="plus" size={17}/>{t('add')}</Link>}</div>
        {error && <div className="dashboard-error"><Icon name="alert" size={16}/>{error}</div>}
        <section className="kpi-grid">
          <Link href="/documents" className="kpi-card"><div><span>{t('total')}</span><strong>{summary.total}</strong><small>{t('allActive')}</small></div><i className="kpi-icon blue"><Icon name="doc" size={22}/></i></Link>
          <Link href="/documents?status=ACTIVE" className="kpi-card"><div><span>{t('active')}</span><strong>{summary.active}</strong><small>{t('activeDocuments')}</small></div><i className="kpi-icon green"><Icon name="clock" size={22}/></i></Link>
          <Link href="/documents?status=EXPIRING_SOON" className="kpi-card warning"><div><span>{t('expiringSoon')}</span><strong>{summary.expiringSoon}</strong><small>{t('within30')}</small></div><i className="kpi-icon amber"><Icon name="bell" size={22}/></i></Link>
          <Link href="/documents?status=EXPIRED" className="kpi-card danger"><div><span>{t('expired')}</span><strong>{summary.expired}</strong><small>{t('expiredNow')}</small></div><i className="kpi-icon red"><Icon name="alert" size={22}/></i></Link>
        </section>

        <section className="dashboard-columns"><div className="dashboard-left">
          <div className="panel chart-panel"><div className="panel-title"><div><strong>{t('status')}</strong><span>{t('dashboardCopy')}</span></div><button>This month⌄</button></div><div className="status-chart"><div className="donut" style={{ background: statusGradient }}><div><strong>{summary.total}</strong><span>{t('total')}</span></div></div><div className="legend"><div><i className="dot green"/><span>{t('active')}</span><b>{summary.active}</b><small>{Math.round(summary.active / statusTotal * 100)}%</small></div><div><i className="dot amber"/><span>{t('expiringSoon')}</span><b>{summary.expiringSoon}</b><small>{Math.round(summary.expiringSoon / statusTotal * 100)}%</small></div><div><i className="dot red"/><span>{t('expired')}</span><b>{summary.expired}</b><small>{Math.round(summary.expired / statusTotal * 100)}%</small></div><div><i className="dot slate"/><span>{t('noExpiry')}</span><b>{summary.noExpiry}</b><small>{Math.round(summary.noExpiry / statusTotal * 100)}%</small></div></div></div></div>
          <div className="panel chart-panel"><div className="panel-title"><div><strong>{t('timeline')}</strong><span>{t('expiringSoon')} · 30–60 day outlook</span></div><button>This month⌄</button></div><div className="timeline"><div className="grid-lines"><span>40</span><span>30</span><span>20</span><span>10</span><span>0</span></div><div className="bars">{timeline.map(item => <div className="bar-col" key={item.label}><strong>{item.value}</strong><div className="bar" style={{ height: `${item.height}%` }}/><span>{item.label}<br/>days</span></div>)}</div></div></div>
          <div className="panel recent-panel"><div className="panel-title"><div><strong>{t('recent')}</strong><span>{summary.total} documents tracked</span></div><Link href="/documents">{t('viewAll')} →</Link></div><div className="recent-table"><div className="table-head"><span>{t('documents')}</span><span>{t('category')}</span><span>{t('owner')}</span><span>{t('expiry')}</span><span>{t('status')}</span><span/></div>{loading && orderedRecent.length === 0 ? <div className="table-empty">Loading…</div> : orderedRecent.slice(0, 6).map(doc => <Link href={`/documents/${doc.id}`} className="table-row" key={doc.id}><div className="doc-name"><span className="file-icon"><Icon name="doc" size={15}/></span><strong>{doc.title}</strong></div><span>{doc.documentType}</span><span>{doc.counterparty || '—'}</span><span className={doc.status === 'EXPIRED' || doc.status === 'EXPIRING_SOON' ? 'date-alert' : ''}>{formatDate(doc.expiryDate, lang)}</span><span><em className={`status-pill ${doc.status.toLowerCase()}`}>{statusLabel(doc.status, t)}</em></span><span>•••</span></Link>)}{!loading && orderedRecent.length === 0 && <div className="table-empty">{t('noDocs')} <Link href="/documents/new">{t('addFirst')}</Link></div>}</div></div>
        </div><aside className="dashboard-right">
          <div className="panel attention-panel"><div className="panel-title"><div><strong>{t('attention')}</strong><span>{t('attentionSub')}</span></div><Link href="/documents?status=EXPIRING_SOON">{t('viewAll')}</Link></div><div className="attention-list">{attention.length ? attention.map(doc => { const d = daysUntil(doc.expiryDate); return <Link href={`/documents/${doc.id}`} className="attention-row" key={doc.id}><span className={`attention-icon ${doc.status.toLowerCase()}`}><Icon name={doc.status === 'EXPIRED' ? 'alert' : 'doc'} size={16}/></span><div><strong>{doc.title}</strong><small>{doc.documentType} · {doc.counterparty || '—'}</small></div><div className="attention-date"><em>{d === null ? '—' : d < 0 ? `${Math.abs(d)} ${t('day')} ${t('expiredLabel')}` : `${d} ${d === 1 ? t('day') : t('days')}`}</em><span>{formatDate(doc.expiryDate, lang)}</span></div></Link>); }) : <div className="empty-attention"><Icon name="clock" size={24}/><strong>{t('allClear')}</strong><span>{t('noAttention')}</span></div>}</div><div className="attention-footer">{t('viewAll')} {t('attention').toLowerCase()} →</div></div>
          <div className="panel quick-panel"><div className="panel-title"><div><strong>{t('quick')}</strong><span>Common actions</span></div></div><div className="quick-grid">{(role === 'SUPERUSER' || role === 'EDITOR') && <Link href="/documents/new"><Icon name="plus" size={21}/><strong>{t('add')}</strong></Link>}<Link href="/documents/new"><Icon name="upload" size={21}/><strong>{t('upload')}</strong></Link><Link href="/documents?status=EXPIRING_SOON"><Icon name="clock" size={21}/><strong>{t('attention')}</strong></Link><Link href="/documents"><Icon name="search" size={21}/><strong>{t('searchAction')}</strong></Link></div></div>
          <div className="panel insight-panel"><div className="panel-title"><div><strong>{t('noExpiry')}</strong><span>Documents without a deadline</span></div></div><div className="insight-number"><strong>{summary.noExpiry}</strong><span>{t('noExpiry')}</span></div><div className="insight-track"><i style={{ width: `${Math.min(100, summary.noExpiry / Math.max(summary.total, 1) * 100)}%` }}/></div><small>{summary.noExpiry} of {summary.total} documents</small></div>
        </aside></section>
      </div></section>
    <style jsx global>{`
      .premium-app{min-height:100vh;display:grid;grid-template-columns:250px minmax(0,1fr);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.premium-sidebar{position:sticky;top:0;height:100vh;padding:24px 16px;display:flex;flex-direction:column;color:#eaf0f8;box-shadow:inset -1px 0 rgba(255,255,255,.05)}.premium-brand{display:flex;gap:11px;align-items:center;padding:4px 8px 30px}.premium-brand>div{display:grid;line-height:1.2}.premium-brand strong{font-size:12px;letter-spacing:-.01em}.premium-brand span{font-size:11px;color:#8d9ab0;margin-top:3px}.dash-brand-mark{width:36px;height:36px;display:inline-flex;flex:none}.dash-brand-mark svg{width:100%;height:100%}.sidebar-label{padding:0 12px 9px;font-size:9px;font-weight:800;letter-spacing:.13em;color:#6f7d92}.admin-label{margin-top:22px}.premium-sidebar nav{display:grid;gap:4px}.premium-nav{height:42px;padding:0 12px;display:flex;align-items:center;gap:12px;border-radius:9px;color:#a6b1c1;font-size:12px;font-weight:650}.premium-nav:hover{background:rgba(255,255,255,.06);color:#fff}.premium-nav.active{background:#315bc7;color:#fff;box-shadow:0 8px 24px rgba(27,76,190,.22)}.premium-nav svg{flex:none}.sidebar-account{margin-top:auto;padding:11px 9px;border:1px solid rgba(255,255,255,.08);border-radius:11px;display:flex;align-items:center;gap:9px;background:rgba(255,255,255,.035)}.sidebar-account>div:nth-child(2){min-width:0;display:grid;flex:1}.sidebar-account strong{font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.sidebar-account span{font-size:9px;color:#7f8da1;margin-top:2px}.sidebar-account button{border:0;background:transparent;color:#8290a3}.avatar{width:31px;height:31px;border-radius:50%;display:grid;place-items:center;background:linear-gradient(145deg,#4c6fff,#7b5cff);color:#fff;font-size:9px;font-weight:800;flex:none}.premium-main{min-width:0}.premium-header{height:76px;padding:0 clamp(20px,3vw,38px);display:flex;align-items:center;justify-content:space-between;gap:20px;border-bottom:1px solid var(--border);background:var(--surface);position:sticky;top:0;z-index:5}.header-search{width:min(510px,50vw);height:40px;display:flex;align-items:center;gap:10px;border:1px solid var(--border);border-radius:9px;padding:0 11px;color:var(--muted);background:color-mix(in srgb,var(--surface),#eef2f7 35%)}.header-search input{border:0;outline:0;background:transparent;color:inherit;min-width:0;flex:1;font-size:11px}.header-search kbd{font-size:9px;border:1px solid var(--border);border-radius:5px;padding:3px 6px}.header-actions{display:flex;align-items:center;gap:7px}.header-actions>button{height:36px;border:1px solid var(--border);border-radius:8px;background:var(--surface);color:var(--muted);font-size:10px;font-weight:800;padding:0 10px}.header-actions .notification{width:36px;padding:0;position:relative}.notification b{position:absolute;right:-4px;top:-5px;min-width:15px;height:15px;padding:0 3px;display:grid;place-items:center;border-radius:999px;background:#ef4444;color:#fff;font-size:8px}.header-user{height:40px;padding-left:9px;margin-left:4px;border-left:1px solid var(--border);display:flex;align-items:center;gap:9px}.header-user>div:nth-child(2){display:grid}.header-user strong{font-size:10px}.header-user span{font-size:9px;color:var(--muted);margin-top:2px}.header-user>button{border:0;background:transparent;color:var(--muted)}.premium-content{width:min(100%,1480px);margin:0 auto;padding:30px clamp(20px,3vw,46px) 50px}.page-heading{display:flex;align-items:end;justify-content:space-between;gap:20px;margin-bottom:24px}.page-heading p{margin:0 0 6px;color:var(--muted);font-size:10px;font-weight:700}.page-heading h1{margin:0;font-size:28px;letter-spacing:-.04em}.page-heading span{display:block;margin-top:6px;color:var(--muted);font-size:11px}.add-button{height:39px;display:flex;align-items:center;gap:8px;padding:0 14px;border-radius:9px;background:#315bc7;color:#fff;font-size:11px;font-weight:800;box-shadow:0 8px 20px rgba(49,91,199,.18)}.dashboard-error{margin-bottom:16px;border:1px solid #f2b8bd;background:#fff1f2;color:#bd3038;border-radius:9px;padding:10px 12px;font-size:11px;display:flex;align-items:center;gap:8px}.kpi-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:16px}.kpi-card{min-height:112px;padding:18px 17px;display:flex;justify-content:space-between;align-items:center;border:1px solid var(--border);border-radius:13px;background:var(--surface);box-shadow:0 7px 24px rgba(20,32,56,.045);transition:transform .2s,box-shadow .2s}.kpi-card:hover{transform:translateY(-2px);box-shadow:0 12px 28px rgba(20,32,56,.08)}.kpi-card>div{display:grid}.kpi-card span{font-size:10px;color:var(--muted);font-weight:650}.kpi-card strong{font-size:29px;letter-spacing:-.045em;margin:10px 0 4px}.kpi-card small{font-size:9px;color:var(--muted)}.kpi-card.warning strong{color:#dc8b08}.kpi-card.danger strong{color:#e0444b}.kpi-icon{width:42px;height:42px;border-radius:11px;display:grid;place-items:center}.kpi-icon.blue{background:#edf2ff;color:#315bc7}.kpi-icon.green{background:#e9f8f0;color:#21a66e}.kpi-icon.amber{background:#fff5e3;color:#e99a17}.kpi-icon.red{background:#fff0f1;color:#e0444b}.dashboard-columns{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(300px,.7fr);gap:16px}.dashboard-left{min-width:0;display:grid;gap:16px}.dashboard-right{min-width:0;display:grid;align-content:start;gap:16px}.panel{border:1px solid var(--border);border-radius:13px;background:var(--surface);box-shadow:0 7px 24px rgba(20,32,56,.045);overflow:hidden}.panel-title{min-height:61px;padding:15px 18px;display:flex;align-items:center;justify-content:space-between;gap:12px;border-bottom:1px solid var(--border)}.panel-title>div{display:grid}.panel-title strong{font-size:13px;letter-spacing:-.015em}.panel-title span{font-size:9px;color:var(--muted);margin-top:4px}.panel-title button{border:1px solid var(--border);border-radius:7px;background:var(--surface);color:var(--muted);font-size:9px;padding:6px 8px}.panel-title>a{color:#315bc7;font-size:10px;font-weight:750}.status-chart{min-height:235px;padding:22px;display:flex;align-items:center;justify-content:center;gap:clamp(35px,7vw,90px)}.donut{width:170px;height:170px;border-radius:50%;display:grid;place-items:center;flex:none}.donut>div{width:112px;height:112px;border-radius:50%;background:var(--surface);display:grid;place-content:center;text-align:center}.donut strong{font-size:26px;letter-spacing:-.04em}.donut span{font-size:9px;color:var(--muted);margin-top:2px}.legend{min-width:240px;display:grid;gap:13px}.legend>div{display:grid;grid-template-columns:10px 1fr 35px 35px;align-items:center;gap:7px;font-size:10px}.legend b{text-align:right}.legend small{color:var(--muted);text-align:right;font-size:9px}.dot{width:8px;height:8px;border-radius:50%}.dot.green{background:#27b77a}.dot.amber{background:#f59f20}.dot.red{background:#ef4444}.dot.slate{background:#aab4c3}.timeline{height:235px;padding:20px 22px 17px;display:flex;position:relative}.grid-lines{position:absolute;inset:20px 25px 48px 42px;display:grid;grid-template-rows:repeat(5,1fr);pointer-events:none}.grid-lines span{font-size:8px;color:var(--muted);border-bottom:1px dashed var(--border);position:relative}.grid-lines span:first-child{border-bottom:1px solid var(--border)}.bars{position:relative;z-index:1;margin-left:42px;flex:1;display:grid;grid-template-columns:repeat(4,1fr);align-items:end;gap:20px;padding-bottom:31px}.bar-col{height:100%;display:flex;flex-direction:column;justify-content:end;align-items:center;gap:6px}.bar-col strong{font-size:10px}.bar{width:min(46px,45%);min-height:12px;border-radius:6px 6px 3px 3px;background:#3cb879;box-shadow:0 5px 12px rgba(60,184,121,.15)}.bar-col:nth-child(2) .bar{background:#f5a21b}.bar-col:nth-child(3) .bar{background:#ef4b51}.bar-col:nth-child(4) .bar{background:#8b96a7}.bar-col>span{position:absolute;bottom:0;text-align:center;color:var(--muted);font-size:8px;line-height:1.35}.recent-table{width:100%;overflow:auto}.table-head,.table-row{min-width:760px;display:grid;grid-template-columns:2.2fr 1fr 1fr 1fr .95fr 24px;align-items:center;gap:10px;padding:0 18px}.table-head{height:37px;background:color-mix(in srgb,var(--surface),#eef2f7 35%);color:var(--muted);font-size:8px;font-weight:750;text-transform:uppercase;letter-spacing:.05em}.table-row{min-height:53px;border-top:1px solid var(--border);font-size:9px;color:var(--muted)}.table-row:hover{background:color-mix(in srgb,var(--surface),#f5f7fa 55%)}.table-row>span{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.doc-name{min-width:0;display:flex;align-items:center;gap:9px}.doc-name strong{color:var(--text);font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.file-icon{width:27px;height:27px;border-radius:7px;display:grid;place-items:center;background:#edf2ff;color:#315bc7;flex:none}.date-alert{color:#e58c0d;font-weight:700}.status-pill{font-style:normal;font-size:8px;font-weight:800;padding:4px 7px;border-radius:999px;white-space:nowrap}.status-pill.active{background:#e8f8ef;color:#219b67}.status-pill.expiring_soon{background:#fff4df;color:#d98a08}.status-pill.expired{background:#fff0f1;color:#d83f47}.status-pill.archived{background:#eef1f5;color:#667085}.status-pill.no_expiry{background:#eef1f5;color:#667085}.table-empty{padding:34px 20px;text-align:center;color:var(--muted);font-size:10px}.table-empty a{color:#315bc7}.attention-list{padding:5px 0}.attention-row{display:grid;grid-template-columns:32px minmax(0,1fr) auto;align-items:center;gap:9px;padding:12px 14px;border-bottom:1px solid var(--border)}.attention-row:hover{background:color-mix(in srgb,var(--surface),#f5f7fa 55%)}.attention-icon{width:32px;height:32px;border-radius:9px;display:grid;place-items:center}.attention-icon.expiring_soon{background:#fff4df;color:#e79a1b}.attention-icon.expired{background:#fff0f1;color:#e0444b}.attention-row>div:nth-child(2){min-width:0;display:grid}.attention-row strong{font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.attention-row small{font-size:8px;color:var(--muted);margin-top:3px}.attention-date{display:grid;justify-items:end}.attention-date em{font-style:normal;font-size:8px;font-weight:800;color:#dc8b08;background:#fff4df;border-radius:999px;padding:4px 6px}.attention-date span{font-size:8px;color:var(--muted);margin-top:4px}.attention-footer{padding:11px 15px;text-align:right;color:#315bc7;font-size:9px;font-weight:750}.empty-attention{min-height:160px;display:grid;place-content:center;justify-items:center;gap:7px;color:var(--muted);text-align:center;padding:20px}.empty-attention svg{color:#27b77a}.empty-attention strong{color:var(--text);font-size:11px}.empty-attention span{font-size:9px;max-width:210px;line-height:1.5}.quick-grid{padding:12px;display:grid;grid-template-columns:1fr 1fr;gap:8px}.quick-grid a{min-height:76px;border:1px solid var(--border);border-radius:9px;display:grid;place-content:center;justify-items:center;gap:7px;color:#315bc7;background:color-mix(in srgb,var(--surface),#f5f7fa 35%)}.quick-grid a:hover{border-color:#b9c8ef;transform:translateY(-1px)}.quick-grid strong{font-size:9px;color:var(--text)}.insight-panel{padding-bottom:16px}.insight-number{padding:18px 18px 8px;display:flex;align-items:end;gap:8px}.insight-number strong{font-size:31px;letter-spacing:-.04em}.insight-number span{font-size:9px;color:var(--muted);padding-bottom:5px}.insight-track{height:7px;margin:4px 18px 8px;border-radius:99px;background:#edf0f4;overflow:hidden}.insight-track i{display:block;height:100%;border-radius:99px;background:#8b96a7}.insight-panel>small{padding:0 18px;color:var(--muted);font-size:8px}@media(max-width:1120px){.premium-app{grid-template-columns:215px minmax(0,1fr)}.dashboard-columns{grid-template-columns:1fr}.dashboard-right{grid-template-columns:1.2fr .8fr}.insight-panel{grid-column:1/-1}.kpi-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:760px){.premium-app{display:block}.premium-sidebar{position:relative;height:auto;padding:12px;display:block}.premium-brand{padding-bottom:12px}.premium-sidebar nav,.sidebar-label,.admin-label,.sidebar-account{display:none}.premium-sidebar:after{content:'Overview   Documents   Reminders   Archived';display:block;color:#9eabbd;font-size:10px;padding:10px 8px;border-top:1px solid rgba(255,255,255,.08)}.premium-header{height:64px;position:relative;padding:0 14px}.header-search{width:42px;border:0;background:transparent;padding:0}.header-search input,.header-search kbd{display:none}.header-actions{margin-left:auto}.header-actions>button{padding:0 8px}.header-user>div:nth-child(2),.header-user>button{display:none}.premium-content{padding:20px 13px 35px}.page-heading{align-items:start}.page-heading h1{font-size:23px}.page-heading span{line-height:1.5}.add-button{font-size:0;width:39px;justify-content:center;padding:0}.kpi-grid{grid-template-columns:1fr 1fr;gap:9px}.kpi-card{min-height:100px;padding:14px}.kpi-card strong{font-size:24px}.kpi-icon{width:35px;height:35px}.dashboard-right{grid-template-columns:1fr}.status-chart{gap:18px;padding:18px;min-height:205px}.donut{width:125px;height:125px}.donut>div{width:83px;height:83px}.donut strong{font-size:20px}.legend{min-width:0;flex:1}.timeline{height:205px}.panel-title{padding:13px 14px}.login-showcase{display:none!important}.premium-login{display:block!important}.login-panel{min-height:100vh!important}.login-card{width:min(100% - 28px,420px)!important;padding:28px!important}.header-user{padding-left:0;border-left:0}.notification{display:none!important}}
    `}</style>
  </main>;
}
